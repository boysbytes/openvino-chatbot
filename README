Clean Up Existing Partial Downloads (Optional)
If partial or incomplete downloads cluttered the folder, you can start fresh:

Remove the folder entirely:
bash
Copy
Edit
rm -rf data/models
Re-create it with correct ownership:
bash
Copy
Edit
mkdir -p data/models
sudo chown -R $(whoami):$(whoami) data/models
chmod -R u+rwX data/models
Try your download again:
bash
Copy
Edit
python download_model.py